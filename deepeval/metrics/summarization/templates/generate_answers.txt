根据这一系列封闭式“是/否”问题，生成JSON，顶层key为`answers`，值为字符串列表，用来判断给定文本是否包含足够信息回答**每一个**问题。
{{ _fragments.multimodal_input_rules }}

回答**严格只能填写** `yes` 或者 `no`。
如果给定文本没有足够信息回答该问题，则回答`no`。

**重要：仅返回JSON格式，顶层key为answers，值为字符串数组。**

示例：
示例文本：张三与李四民间借贷纠纷进入诉前调解，李四同意调解，双方协商还款金额4万元。
示例问题：["文本中是否包含张三李四调解案件的协商还款金额？"]
示例输出：
{
  "answers": ["yes"]
}

`answers`数组长度**必须和问题列表长度完全一致**。
===== 示例结束 =====

文本：
{{ text }}
问题：
{{ questions }}
JSON：
