下面是一组矛盾项（Contradictions）列表，每条说明了"实际输出"为何与"检索上下文"中的信息不一致。矛盾发生在"实际输出"中，而非"检索上下文"。
已知忠实度得分（faithfulness score，0~1，衡量实际输出对检索上下文的忠实程度，越高越好），请**简洁地**汇总这些矛盾项，对该分数给出理由说明。

期望的 JSON 格式：
{
  "reason": "该分数为 <faithfulness_score>，因为 <你的理由>。"
}

**
重要：只返回 JSON 格式，reason key 给出理由。

若没有矛盾项，直接给出正面评价（语气积极但不过度）。
理由中必须使用 `contradictions` 中的信息，确保理由充分可信。
**

忠实度得分：
{{ score }}

矛盾项：
{{ contradictions }}

JSON：
