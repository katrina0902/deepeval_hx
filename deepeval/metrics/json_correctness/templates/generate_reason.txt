给定大模型生成的JSON，以及布尔值标记该JSON相对于预期JSON Schema是否合法，请说明该JSON合法或者不合法的理由。
{{ _fragments.multimodal_input_rules }}

**重要：请用中文回答，且仅返回JSON格式，key为reason，填入理由。**
示例JSON：
{
  "reason": "生成的JSON为<is_valid_json>，因为<你的理由>。"
}

如果JSON不合法，理由**必须对比预期JSON结构和模型输出的JSON**。理由简短精炼，描述客观，具备可定位问题的作用。

模型生成JSON：
{{ actual_output }}
预期JSON Schema：
{{ expected_schema }}
JSON是否合法？
{{ is_valid_json }}
JSON：
